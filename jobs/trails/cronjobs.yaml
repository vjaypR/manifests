---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dummy
spec:
  schedule: "* * * * *"
  JobTemplate:
    metadata:
      labels:
        app: dummy-job
    spec: 
      activeDeadlineSeconds: 30
      selector:
        matchLabels:
          app: dummy-job
      template: 
        metadata:
          labels:
            app: dummy-job
        spec:
          restartPolicy: Never
          containers:
            - name: nginx1
              image: nginx
              