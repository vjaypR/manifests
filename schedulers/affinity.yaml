---
apiVersion: v1 
kind: Pod
metadata:
  name: affintiy
  labels:
    app: web
spec:
  affinity:
    podAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchLabels:
              app: basepod
          topologyKey: kubernetes.io/hostname
  containers:
    - name: affinity
      image: nginx
      resources:
        limits:
          cpu: "500m"
          memory: "128Mi"