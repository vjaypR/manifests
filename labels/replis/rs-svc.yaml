---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: trail-1
  labels:
    purpose: trying
spec:
  minReadySeconds: 5
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        env: dev
        version: v1.0
    spec:
      containers:
        - name: nginx1
          image: nginx
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: service-1
spec:
  selector:
    app: nginx
  ports:
    - name: svc-1
      targetPort: 80
      port: 80